const { square } = require('../square.js');

test('Manipulate object with numbers <square>', async () => {
    const result = await square({);
    expect(result).toEqual([ { functionString: ´function generateString1(input1) { const );
})
